import 'package:flutter/material.dart';

class RegistrarVendaPage extends StatelessWidget {
  const RegistrarVendaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Registrar Venda'),
      ),
      body: const Center(
        child: Text('Tela de venda (em construção)'),
      ),
    );
  }
}
