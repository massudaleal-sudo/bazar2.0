name: bazar_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔑 Firebase Core (obrigatório para inicializar o Firebase)
  firebase_core: ^4.3.0

  # 📂 Firestore (para banco de dados)
  cloud_firestore: ^6.1.1

  # 🔐 Authentication (para login de administrador)
  firebase_auth: ^6.1.3
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
